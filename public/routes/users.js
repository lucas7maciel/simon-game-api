/*module.exports = (app) => {
  const funcs = app.public.controllers.users

  app.post('/users/signup', funcs.signUp)
  app.get('/users/signin/:nick/:password', funcs.signIn) //tirar esses argumentos na url
}*/